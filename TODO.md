# TODO

- [x] 2026-09-24 — Catálogos — `20260925032853_import_doptex_workwear_fabrics.sql`, `legacy-catalog-fallback.json`, `doptex-workwear-fabrics.test.ts`: extraídos do catálogo Workwear 2024 da Doptex os 29 artigos (somente nome, largura e composição). Oito equivalentes de camisaria foram atualizados no mesmo `catalog_items.id` e os demais inseridos, sem cores, gramatura ou instruções de lavagem; aliases técnicos existentes foram preservados. Como a largura exige o par técnico do Plano de Corte, todos foram marcados internamente como tecido `PLANO`, coerente com o catálogo. Migração aplicada no Supabase remoto em transação e registrada em `supabase_migrations.schema_migrations`; conferência remota confirmou as 29 linhas e seus valores exatos. Typecheck, lint (uma advertência preexistente em `plano-de-corte-solver.test.ts:677`), build, Supabase check, teste focado e encoding passaram. A suíte completa ficou em 133/134 por uma falha preexistente de impressão em `code-review-hardening.test.ts`, já registrada neste TODO.

- [x] Aplicar e validar no Supabase remoto a migration `20260910012830_client_identity_and_explicit_creation.sql` após revisão dos dados de produção.

- [x] Plano de corte: orçamento global de até 30 segundos, execução em worker, resultado parcial validado, encerramento com melhor resultado e invalidação ao editar.
- [x] Plano de corte: corrigir aliases antes da paridade, cache dimensional, arredondamento de segmentos, dominância e mesclagem dos contraexemplos da auditoria.
- [x] Plano de corte: corrigir identidade aleatória do tecido inicial na hidratação e verificar correspondência entre HTML do servidor e estado do cliente no navegador.
- [x] Plano de corte: incorporar fallback dimensional versionado para camisetas tradicionais, Baby Look, calças e shorts/bermudas, com precedência das medidas cadastradas, margem por confiança e origem explícita no resultado.
- [x] Plano de corte: incorporar equilíbrio entre quantidades de tamanhos por enfesto ao ranking, calibrado junto ao comprimento para evitar mapas isolados sem inflar desproporcionalmente o marcador.
- [x] Plano de corte: remover saia e macacão do seletor e da classificação de itens importados.
- [x] 2026-09-12 — Plano de corte — `model.ts`, `normalization.ts`, `ficha-item-classification.ts`, editor, API, solver e apresentação: modelagem explícita distingue camiseta e camisa social na identidade, importação, edição e conferência. Camisa social permanece sem fallback dimensional até ter tabela própria.
- [x] 2026-09-12 — Plano de corte — `alternatives.ts`, `merge-solver.ts`, `plano-de-corte-audit.test.ts`: busca conjunta explora divisões locais até o limite útil e poda combinações por limite inferior seguro. Caveat: mesclagens continuam declaradas como viáveis até existir prova global completa.
- [x] 2026-09-12 — Plano de corte — `calculator.ts`, `plano-de-corte-workspace.tsx`, `cut-plan-print-simple.tsx`: tabelas usam a ordem canônica dos tamanhos; tamanhos infantis exibem hífen e o texto operacional copiado usa vírgulas sem parênteses.
- [x] 2026-09-12 — Plano de corte — `scripts/benchmark-cut-plan.mjs`, `scripts/audit-cut-plan-real-orders.mjs`, `package.json`: benchmark reproduzível com pedidos anonimizados, oráculo enumerativo multiobjetivo e medições de tempo, memória e qualidade por orçamento; auditoria de pedidos reais atualizada para preservar a modelagem explícita. Caveat: o heap é uma aproximação do processo Node, sujeito ao coletor de lixo.
- [x] 2026-09-24 — Fichas — `components/ui/floating-menu.tsx`, `ficha-row-actions.tsx`, `print-ficha.tsx`, `print-pdf.ts`, `fichas/[id]/imprimir/page.tsx`: clique direito na linha da ficha abre o menu de ações existente no cursor (`FloatingMenu` ganhou `contextMenuTarget`; Shift+F10 ancora no botão). Novo item "Imprimir lista de nomes" imprime só a `lista_nomes_raw` (lista recebida no pedido) via `?somenteListaNomes=1`, sem a ficha; a rasterização de página única é ignorada quando não há página principal. A página da lista usa a fonte do app (`--font-sans`), antes caía em Courier por `--font-mono` inexistente (`fichas.css`). Caveat: o clique direito substitui o menu nativo em toda a linha, inclusive sobre o link do cliente.
- [x] 2026-09-24 — Plano de corte — `plano-de-corte-workspace.tsx`, `plano-de-corte.css`, `cut-plan-ficha-picker.tsx`, `cut-plan-items-editor.tsx`, `components/ui/alert-dialog.tsx`, `flows.spec.js`: hierarquia visual. Barra de comando sticky no fim do plano com estado (vazio, revisar, pronto, calculando, calculado) e resumo; "Calcular plano" como única ação primária; "Limpar plano" mantém a mesa, "Novo plano" volta tudo ao padrão e foca a etapa 1; etapas neutras com destaque na etapa em edição/concluída e acento reservado ao Resultado; cards de tecido com nome e cor como identidade; "Importar fichas" separado de "Itens do plano"; ações internas em secondary/ghost. Toasts e `scroll-padding` sobem acima da barra enquanto a página está montada. `AlertDialog` ganhou `onCloseAutoFocus` opcional. Solver, validação e cálculo intactos. Caveat: 3 testes de `flows.spec.js` (rótulo "Manga da linha", caixa da cor importada, formato `4M`) e 2 só no mobile (hover e arraste) já falhavam antes e continuam desatualizados.
- [x] 2026-09-24 — Plano de corte — `cut-plan-print-simple.tsx`, `plano-de-corte.css`: impressão compacta. Margem da folha 12/13 mm → 7/8 mm, título, cabeçalhos, cartões e células mais enxutos; fichas anexadas em duas colunas. A paginação manual (máx. 4 tabelas por folha, tabela ≥15 linhas em folha própria) foi removida: cada bloco usa `break-inside: avoid` e só quebra quando não cabe. A conferência final ocupa a metade livre ao lado de um último enfesto sozinho (exceto em enfestos mesclados, cuja tabela tem 5 colunas). Verificado em PDF A4: 3 enfestos + 13 tamanhos e 9 enfestos + 17 tamanhos cabem numa folha.
- [x] 2026-09-24 — Fichas — `operational-report.tsx` (novo), `fichas/relatorio/imprimir/page.tsx` (novo), `print-period.ts` (novo), `fichas-filter-toolbar.tsx`, `fichas-overview.tsx`, `proxy.ts`, `fichas.css`, `code-review-hardening.test.ts`; removidos `fichas/pdf/route.ts` e `operational-pdf.ts`: o relatório operacional deixou de ser PDF (jsPDF) e virou impressão direta em HTML pelo mesmo fluxo da ficha individual (`PrintTriggerButton` → iframe → `PrintOnLoad`/`printFichaAutomatically`); botão "Imprimir" no lugar de "Exportar PDF". Período obrigatório (entrega inicial e final, máx. 62 dias, final ≥ inicial) validado na página (server) e na toolbar (toast + foco); antes, exportar sem período baixava o banco inteiro. Layout: cabeçalho de duas linhas, tabela com grade e `thead` repetido por página, seções Eventos (destacadas, no topo) → Atrasadas → período, agrupadas por personalização sem diferenciar caixa; coluna Status em branco para anotação manual (entregues preenchidas); sem contagem de dias de atraso. "Incluir atrasadas" vale para qualquer período. Verificado no navegador com dados reais (A4, 2 páginas). Caveat: imprime todas as fichas do período, não só a página da tela; se a consulta falhar dentro do iframe, o toast de preparo some após 45 s sem erro explícito; teste `impressão permite o frame blob…` falha por mudança não commitada anterior em `print-pdf.ts`.
- [x] 2026-09-24 — Clientes — migration `20260924120000_uppercase_client_names.sql`, `lib/name-normalizer.ts`, `clientes/cliente-form.tsx`, `fichas/cliente-picker.tsx`, `fichas/actions.ts`, `fichas/ficha-draft-print.tsx`: nomes de clientes em MAIÚSCULAS. `public.normalize_client_name` (espaços colapsados + `upper`) substitui o Title Case nos triggers de `clientes.nome` (reativado; a migration de identidade o havia removido) e `fichas.cliente_nome_snapshot`; backfill de ambos sem tocar `updated_at`. Inputs de nome forçam maiúsculas ao digitar/colar (`forceUppercaseInput`, preserva cursor). Identidade/unicidade usam `lower()`, então não há novas colisões. Caveat: a caixa Title Case antiga não é recuperável depois do backfill.
- [x] 2026-09-24 — Fichas — `operational-report.tsx`: no relatório impresso, fichas com mesmo cliente + número de venda + entrega (dentro da mesma personalização) viram uma linha `NOME (N Fichas)` (número de venda não é exibido; coluna Cliente 39% → 46%); "Entregue" só quando todas estão entregues; vendedor e início vêm da primeira ficha. Contagens de seção/grupo continuam em fichas.
- [x] 2026-09-24 — Fichas — `data.ts`, `print-ficha.tsx`, `fichas/[id]/imprimir/page.tsx`: o "por X" da ficha impressa mostra o autor (`created_by_user_id` → `app_users.display_name`, primeiro nome) em vez de quem imprimiu; vale também para a prévia em modal. Sem autor gravado (ficha antiga ou rascunho), cai para o usuário logado via prop `fallbackAuthor` (impressão, prévia e rascunho do formulário). Rascunho não tem número e sai como `Ficha por X` (antes saía `#NaN`).
- [x] 2026-09-24 — Clientes — migration `20260924120000_uppercase_client_names.sql` aplicada no Supabase remoto via `pg` (só esse arquivo; `db push` não é seguro porque o histórico remoto está dessincronizado das migrations locais antigas) e registrada em `supabase_migrations.schema_migrations`. 403/438 clientes e 820/827 fichas convertidos, 0 pendentes; triggers `*_set_updated_at` reabilitados.
- [x] 2026-09-24 — Fichas — `ficha-form.tsx`, `observacoes-autofill.ts` (uso), `actions.ts`, `ficha-draft-print.tsx`, `styles/domains/base.css`: editor de observações. O `onUpdate` do Tiptap fazia `setContent` com o HTML em maiúsculas a cada tecla, recriando o documento e jogando o cursor para o fim; agora o editor guarda o HTML como digitado (o CSS já exibe em maiúsculas) e `uppercaseObservationHtml` é aplicado no save (server action) e na impressão de rascunho. O placeholder era `::before` na raiz do editor e virava uma linha extra não editável quando o conteúdo esvaziava (ex.: Ctrl+Backspace); passou para o primeiro bloco com `float`/`height: 0` e `data-empty` usa `editor.isEmpty`. Verificado no navegador (inserção no meio do texto e Ctrl+Backspace até vazio). Caveat: o texto fica em minúsculas no DOM/estado até salvar; cópia do editor antes de salvar sai como digitado.
- [x] 2026-09-24 — Fichas — `schema.ts`, `ficha-form.tsx`, `print-requirements.ts` (novo), `fichas/[id]/imprimir/page.tsx`, `print-on-load.tsx`, `print-trigger-button.tsx`, `quality-tests/ficha-required-rules.test.ts` (novo): "Acabamento da manga" e "Largura da gola" obrigatórios quando visíveis (manga fora do modo regata/colete; largura quando há acabamento da gola e a gola não é polo/social). Regra única `getMissingConditionalFields` validada no server (`superRefine`) e reusada no client para não subir imagens antes do erro. Impressão bloqueada sem layout (imagem), exceto "Sem Personalização": a página `/imprimir` recusa no server (exceto `somenteListaNomes`) e avisa o `PrintTriggerButton` via `postMessage` com `error` (toast "Impressão bloqueada"); o rascunho da ficha nova checa no client. Verificado no navegador com fichas reais. Caveats: fichas antigas sem esses campos passam a exigir o preenchimento ao editar; 6 fichas atuais sem imagem e com personalização ficam sem impressão até anexar layout; salvar continua exigindo imagem mesmo para "Sem Personalização".
- [x] 2026-09-24 — Fichas — `product-item-sorting.ts`, `ficha-form.tsx`, `ficha-product-item-sorting.test.ts`: "Ordenar por tamanho" e o salvamento usam a mesma regra estável: agrupam pela primeira aparição dos detalhes, depois pela primeira aparição do produto dentro deles e só então aplicam a ordem canônica dos tamanhos, preservando a separação tradicional/masculina e Baby Look/feminina já definida no domínio.
- [x] 2026-09-24 — Fichas — migration `20260924130000_manga_longa_acabamento.sql`, `database.types.ts`, `schema.ts`, `form-state.ts`, `actions.ts`, `ficha-form-seed.ts`, `ficha-form.tsx`, `ficha-draft-print.tsx`, `observacoes-autofill.ts`, `print-ficha.tsx`, `ficha-preview.tsx`, `ficha-required-rules.test.ts`: manga "Curta e Longa" ganha acabamento, largura e cor próprios para a manga longa (`acabamento_manga_longa`, `largura_manga_longa`, `cor_acabamento_manga_longa`); as colunas existentes passam a ser da manga curta nesse caso (rótulos "(curta)"/"(longa)"). Acabamento da longa obrigatório (server + client); colunas longas só são gravadas quando a manga é "Curta e Longa". O wrapper `save_ficha_atomic` grava as novas colunas na mesma transação, sem reescrever `save_ficha_atomic_unchecked`. Observações: `MANGA CURTA COM … E MANGA LONGA COM …`. Rótulo "Largura da manga" corrigido para "Largura do acabamento". Caveat: não verificado no navegador.
- [x] 2026-09-24 — Fichas — migration `20260924130000_manga_longa_acabamento.sql` aplicada no Supabase remoto via `pg` numa transação (mesmo motivo de não usar `db push`) e registrada em `supabase_migrations.schema_migrations`. Conferido: 3 colunas criadas e wrapper `save_ficha_atomic` atualizado; o wrapper remoto era idêntico ao de `20260731203921` antes da troca.
- [ ] Plano de corte: validar a eficiência estimada de encaixe com arquivos e resultados reais do Audaces; depende de um corpus exportado do Audaces com comprimento observado por marcador.
- [ ] Plano de corte: continuar calibrando com resultados reais do Audaces a penalidade de equilíbrio, a retirada de `sizeSpreadScore` e a prioridade de folhas versus fragmentação/reutilização de marcadores; introduzir alternativas de Pareto após consolidar a política.
- [x] 2026-09-12 — Plano de corte — `src/features/plano-de-corte/solver.ts`, `src/features/plano-de-corte/merge-solver.ts`: o limite de 150 mil estados deixou de encerrar a busca; a DP compacta deterministicamente os 100 mil estados parciais mais promissores e continua com prova marcada como incompleta, enquanto o cache de mesclagem pode ser reiniciado sem perder correção. Caveat: uma DP compactada preserva a melhor solução encontrada, mas não declara otimalidade.
